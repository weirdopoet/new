import { jsx as _jsx, Fragment as _Fragment, jsxs as _jsxs } from "react/jsx-runtime";
import { resolveScheme } from "../../../../../utils/ipfs.js";
import { useCustomTheme } from "../../../../core/design-system/CustomThemeProvider.js";
import { radius } from "../../../../core/design-system/index.js";
import { Container } from "../../components/basic.js";
import { Spacer } from "../../components/Spacer.js";
import { Text } from "../../components/text.js";
export function WithHeader({ children, uiOptions, defaultTitle, client, }) {
    const theme = useCustomTheme();
    return (_jsxs(Container, { flex: "column", children: [uiOptions.metadata?.image && (_jsx("div", { style: {
                    aspectRatio: "16/9",
                    backgroundColor: theme.colors.tertiaryBg,
                    backgroundImage: `url(${getUrl(client, uiOptions.metadata.image)})`,
                    backgroundPosition: "center",
                    backgroundSize: "cover",
                    borderRadius: `${radius.md} ${radius.md} 0 0`,
                    overflow: "hidden",
                    width: "100%",
                } })), _jsxs(Container, { flex: "column", px: "lg", children: [_jsx(Spacer, { y: "lg" }), _jsx(Text, { color: "primaryText", size: "lg", weight: 700, children: uiOptions.metadata?.title || defaultTitle }), uiOptions.metadata?.description && (_jsxs(_Fragment, { children: [_jsx(Spacer, { y: "xs" }), _jsx(Text, { color: "secondaryText", size: "sm", children: uiOptions.metadata?.description })] })), _jsx(Spacer, { y: "lg" }), children] })] }));
}
function getUrl(client, uri) {
    if (!uri.startsWith("ipfs://")) {
        return uri;
    }
    return resolveScheme({
        client,
        uri,
    });
}
//# sourceMappingURL=WithHeader.js.map